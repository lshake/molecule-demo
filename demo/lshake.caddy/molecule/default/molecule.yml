---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: caddytest
    image: "lshake/${MOLECULE_DISTRIBUTION:-centos7}-systemd:latest"
    command: ${MOLECULE_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: true
    pre_build_image: true
provisioner:
  name: ansible
  #  inventory:
  #    host_vars:
  #      localhost:
  #        ansible_python_interpreter: /usr/bin/python
  lint:
    name: ansible-lint
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
